syntax = "proto3";

package handtracking;

service VideoStream {
    rpc SendFrame(stream Frame) returns (StreamAck);
}

message Frame {
    bytes left = 1;
    bytes right = 2;
}

message StreamAck {
    string message = 1;
}
